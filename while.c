#include<stdio.h>
int main(){
    int contador = 1;

    while (contador <= 5)//enquanto isso não acontecer fazer isso até acontecer
    {
        printf("%i\n",contador);
        contador++;
    }
    

    return 0;
}